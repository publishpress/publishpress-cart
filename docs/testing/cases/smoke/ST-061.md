---
suite: smoke
edition: both
module: smoke-subscriptions
features:
  - subscriptions
priority: high
title: Monthly subscription initial purchase
tags:
  - subscription
automation:
  implementations:
    - suite: smoke
      file: tests/playwright/smoke/st061-monthly-subscription-initial-purchase.spec.ts
---

# Monthly subscription initial purchase

## Scenario

Automated by `tests/playwright/smoke/st061-monthly-subscription-initial-purchase.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `checkout_subscription_url` is configured for the target site.

## Steps

1. Open the checkout page configured as `checkout_subscription_url`.

## Expected result

1. The page contains "month".

## Edge cases

- None.
