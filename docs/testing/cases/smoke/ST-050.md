---
suite: smoke
edition: both
module: smoke-bumps-upsells
features:
  - order-bumps-upsells
priority: high
title: Order bump accepted
automation:
  implementations:
    - suite: smoke
      file: tests/playwright/smoke/st050-order-bump-accepted.spec.ts
---

# Order bump accepted

## Scenario

Automated by `tests/playwright/smoke/st050-order-bump-accepted.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `checkout_bump_url` is configured for the target site.

## Steps

1. Open the checkout page configured as `checkout_bump_url`.
2. Wait for `#ppcart-orderbump-0 input.ppcart_orderbump` to appear.
3. Click `#ppcart-orderbump-0 label`.

## Expected result

1. The page contains "Add this bonus".
2. `#ppcart-orderbump-0 input.ppcart_orderbump` is checked.

## Edge cases

- None.
