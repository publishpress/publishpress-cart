---
suite: smoke
edition: both
module: smoke-order-management
features:
  - order-management
priority: high
title: Abandoned checkout is not paid
tags:
  - stripe
automation:
  implementations:
    - suite: smoke
      file: tests/playwright/smoke/st085-abandoned-checkout-is-not-paid.spec.ts
---

# Abandoned checkout is not paid

## Scenario

Automated by `tests/playwright/smoke/st085-abandoned-checkout-is-not-paid.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `checkout_stripe_url` is configured for the target site.

## Steps

1. Open the checkout page configured as `checkout_stripe_url`.
2. Fill `#email` with the configured `test_email`.

## Expected result

1. The test completes without a failed assertion.

## Edge cases

- None.
