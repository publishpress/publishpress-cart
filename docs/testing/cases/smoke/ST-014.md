---
suite: smoke
edition: both
module: smoke-checkout
features:
  - core-checkout
  - payment-plans
priority: high
title: Single payment plan displays correctly
tags:
  - stripe
automation:
  implementations:
    - suite: smoke
      file: tests/playwright/smoke/st014-single-payment-plan-displays-correctly.spec.ts
---

# Single payment plan displays correctly

## Scenario

Automated by `tests/playwright/smoke/st014-single-payment-plan-displays-correctly.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `checkout_stripe_url` is configured for the target site.

## Steps

1. Open the checkout page configured as `checkout_stripe_url`.

## Expected result

1. The page contains "expected_product_price".
2. `xpath=(//input[@type='radio'][contains(@name,'plan')])[2]` is not rendered.

## Edge cases

- None.
