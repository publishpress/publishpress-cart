---
suite: smoke
edition: both
module: smoke-checkout
features:
  - taxes-vat
priority: high
title: Manual tax applies for taxable address
automation:
  implementations:
    - suite: smoke
      file: tests/playwright/smoke/st039-manual-tax-applies-for-taxable-address.spec.ts
---

# Manual tax applies for taxable address

## Scenario

Automated by `tests/playwright/smoke/st039-manual-tax-applies-for-taxable-address.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `checkout_tax_url` is configured for the target site.

## Steps

1. Open the checkout page configured as `checkout_tax_url`.
2. Fill a taxable billing address and refresh the order total.

## Expected result

1. Manual tax is added to the order total, which equals the expected amount.

## Edge cases

- None.
