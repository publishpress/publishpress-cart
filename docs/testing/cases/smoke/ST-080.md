---
suite: smoke
edition: both
module: smoke-order-management
features:
  - order-management
priority: high
title: Admin order list records successful purchase
tags:
  - admin
automation:
  implementations:
    - suite: smoke
      file: tests/playwright/smoke/st080-admin-order-list-records-successful-purchase.spec.ts
---

# Admin order list records successful purchase

## Scenario

Automated by `tests/playwright/smoke/st080-admin-order-list-records-successful-purchase.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `smoke_admin_path` is configured for the target site.

## Steps

1. Open the checkout page configured as `smoke_admin_path`.

## Expected result

1. The test completes without a failed assertion.

## Edge cases

- None.
