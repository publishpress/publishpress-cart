---
suite: smoke
edition: both
module: smoke-order-management
features:
  - order-management
priority: high
title: Backend status after failed payment
tags:
  - stripe
automation:
  implementations:
    - suite: smoke
      file: tests/playwright/smoke/st087-backend-status-after-failed-payment.spec.ts
---

# Backend status after failed payment

## Scenario

Automated by `tests/playwright/smoke/st087-backend-status-after-failed-payment.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `checkout_stripe_url` is configured for the target site.

## Steps

1. Open the checkout page configured as `checkout_stripe_url`.

## Expected result

1. The test completes without a failed assertion.

## Edge cases

- None.
