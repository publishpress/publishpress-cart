---
suite: smoke
edition: both
module: smoke-bumps-upsells
features:
  - order-bumps-upsells
priority: high
title: First upsell accepted
automation:
  implementations:
    - suite: smoke
      file: tests/playwright/smoke/st054-first-upsell-accepted.spec.ts
---

# First upsell accepted

## Scenario

Automated by `tests/playwright/smoke/st054-first-upsell-accepted.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `checkout_upsell_url` is configured for the target site.

## Steps

1. Open the checkout page configured as `checkout_upsell_url`.
2. Click `xpath=//a[contains(.,'Yes') or contains(.,'Add') or contains(.,'Accept')] | //button[contains(.,'Yes') or contains(.,'Add') or contains(.,'Accept')]`.

## Expected result

1. The test completes without a failed assertion.

## Edge cases

- None.
