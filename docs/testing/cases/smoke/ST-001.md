---
suite: smoke
edition: both
module: smoke-checkout
features:
  - core-checkout
priority: high
title: Stripe one-time checkout succeeds
tags:
  - stripe
automation:
  implementations:
    - suite: smoke
      file: tests/playwright/smoke/st001-stripe-one-time-checkout-succeeds.spec.ts
---

# Stripe one-time checkout succeeds

## Scenario

Automated by `tests/playwright/smoke/st001-stripe-one-time-checkout-succeeds.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `checkout_stripe_url` is configured for the target site.

## Steps

1. Open the checkout page configured as `checkout_stripe_url`.
2. Enter the Stripe test card details.
3. Fill `#first_name` with "GI".
4. Fill `#last_name` with "PublishPress Cart".
5. Fill `#email` with a unique test email address.
6. Fill `#phone` with "5550101001".
7. Click Order Now.

## Expected result

1. The browser lands on a URL carrying a `ppcart-order` id and the order confirmation message is visible.

## Edge cases

- None.
