---
suite: regression
edition: both
module: checkout-paypal
features:
  - subscriptions
  - payment-plans
priority: critical
title: Purchase subscription PayPal checkout flow annually
tags:
  - paypal
extensions:
  gateway: paypal
  purchase_type: subscription
  tax_context: none
automation:
  implementations:
    - suite: regression
      file: tests/playwright/regression/rt015-purchase-subscription-paypal-checkout-flow-annually.spec.ts
---

# Purchase subscription PayPal checkout flow annually

## Scenario

Automated by `tests/playwright/regression/rt015-purchase-subscription-paypal-checkout-flow-annually.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `url_subs_paypal` is configured for the target site and PayPal sandbox credentials are configured.

## Steps

1. Open the checkout page configured as `url_subs_paypal`.
2. Review payment plan 1.
3. Select payment plan 2.
4. Fill the checkout contact fields: first name, last name, a unique email, phone, and company.
5. Choose PayPal as the payment method.
6. Click Order Now.
7. Switch to the PayPal checkout window.
8. Log in to the PayPal sandbox and approve the payment.
9. Return to the merchant site.

## Expected result

1. Payment plan 1 is visible on the checkout form.
2. The browser lands on a URL carrying a `ppcart-order` id and the order confirmation message is visible.

## Edge cases

- None.
