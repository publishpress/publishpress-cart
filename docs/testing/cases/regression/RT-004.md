---
suite: regression
edition: both
module: checkout-stripe
features:
  - core-checkout
  - payment-plans
priority: critical
title: Purchase one-time Stripe checkout flow secondary sale price
tags:
  - stripe
extensions:
  gateway: stripe
  purchase_type: one-time
  tax_context: none
automation:
  implementations:
    - suite: regression
      file: tests/playwright/regression/rt004-purchase-one-time-stripe-checkout-flow-secondary-sale-price.spec.ts
---

# Purchase one-time Stripe checkout flow secondary sale price

## Scenario

Automated by `tests/playwright/regression/rt004-purchase-one-time-stripe-checkout-flow-secondary-sale-price.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

The test is skipped unless `url_one_time_stripe_with_sale_price` is configured for the target site.

## Steps

1. Open the checkout page configured as `url_one_time_stripe_with_sale_price`.
2. Review payment plan 1.
3. Review payment plan 2.
4. Select payment plan 2.
5. Fill the checkout contact fields: first name, last name, a unique email, phone, and company.
6. Enter the Stripe test card details.
7. Click Order Now.

## Expected result

1. Payment plan 1 is visible on the checkout form.
2. Payment plan 2 is visible and shows both its original price and its sale price.
3. The browser lands on a URL carrying a `ppcart-order` id and the order confirmation message is visible.

## Edge cases

- None.
