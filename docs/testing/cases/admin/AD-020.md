---
suite: admin
edition: both
module: admin-settings
features:
  - store-settings
priority: medium
title: Settings General Tab
tags:
  - admin-core
automation:
  implementations:
    - suite: admin
      file: tests/playwright/admin/ad020-settings-general-tab.spec.ts
---

# Settings General Tab

## Scenario

Automated by `tests/playwright/admin/ad020-settings-general-tab.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

## Steps

1. Sign in to WP Admin and open `<site>/wp-admin/admin.php?page=ppcart-settings`.
2. Click `[data-testid="ppcart-admin-settings-tab-general"]`.

## Expected result

1. `[data-testid="ppcart-admin-settings-search"]` is present on the page.
2. `[data-testid="ppcart-admin-settings-tab-general"]` is present on the page.
3. `#content_tab_general` is present on the page.
4. `[data-testid="ppcart-admin-field-ppcart-currency"]` is present on the page.
5. `[data-testid="ppcart-admin-field-ppcart-country"]` is present on the page.
6. `[data-testid="ppcart-admin-field-ppcart-currency-position"]` is present on the page.

## Edge cases

- None.
