---
suite: admin
edition: both
module: admin-settings
features:
  - store-settings
priority: medium
title: Settings Branding Tab
tags:
  - admin-core
automation:
  implementations:
    - suite: admin
      file: tests/playwright/admin/ad022-settings-branding-tab.spec.ts
---

# Settings Branding Tab

## Scenario

Automated by `tests/playwright/admin/ad022-settings-branding-tab.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

## Steps

1. Sign in to WP Admin and open `<site>/wp-admin/admin.php?page=ppcart-settings`.
2. Click `[data-testid="ppcart-admin-settings-tab-branding"]`.

## Expected result

1. `[data-testid="ppcart-admin-settings-search"]` is present on the page.
2. `[data-testid="ppcart-admin-settings-tab-branding"]` is present on the page.
3. `#content_tab_branding` is present on the page.
4. `[data-testid="ppcart-admin-field-ppcart-company-name"]` is present on the page.
5. `[data-testid="ppcart-admin-field-ppcart-company-address"]` is present on the page.
6. `[data-testid="ppcart-admin-field-ppcart-company-logo"]` is present on the page.

## Edge cases

- None.
