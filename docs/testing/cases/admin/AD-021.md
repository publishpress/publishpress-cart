---
suite: admin
edition: both
module: admin-settings
features:
  - store-settings
priority: medium
title: Settings Pages Tab
tags:
  - admin-core
automation:
  implementations:
    - suite: admin
      file: tests/playwright/admin/ad021-settings-pages-tab.spec.ts
---

# Settings Pages Tab

## Scenario

Automated by `tests/playwright/admin/ad021-settings-pages-tab.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

## Steps

1. Sign in to WP Admin and open `<site>/wp-admin/admin.php?page=ppcart-settings`.
2. Click `[data-testid="ppcart-admin-settings-tab-pages"]`.

## Expected result

1. `[data-testid="ppcart-admin-settings-search"]` is present on the page.
2. `[data-testid="ppcart-admin-settings-tab-pages"]` is present on the page.
3. `#content_tab_pages` is present on the page.
4. `[data-testid="ppcart-admin-field-ppcart-myaccount-page-id"]` is present on the page.
5. `[data-testid="ppcart-admin-field-ppcart-terms-url"]` is present on the page.
6. `[data-testid="ppcart-admin-field-ppcart-privacy-url"]` is present on the page.

## Edge cases

- None.
