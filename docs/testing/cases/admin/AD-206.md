---
suite: admin
edition: both
module: admin-workflows
features: []
priority: medium
title: Create Product Tag
tags:
  - admin-workflow
automation:
  implementations:
    - suite: admin
      file: tests/playwright/admin/ad206-create-product-tag.spec.ts
---

# Create Product Tag

## Scenario

Automated by `tests/playwright/admin/ad206-create-product-tag.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

## Steps

1. Sign in to WP Admin and open `<site>/wp-admin/edit-tags.php?taxonomy=ppcart_product_tag&post_type=ppcart_product`.
2. Click `#tag-name`.
3. Set `#tag-name` to "{{admin_workflow_tag_name}}".
4. Click `#tag-slug`.
5. Set `#tag-slug` to "{{admin_workflow_tag_slug}}".
6. Click `#submit`.

## Expected result

1. The test completes without a failed assertion.

## Edge cases

- None.
