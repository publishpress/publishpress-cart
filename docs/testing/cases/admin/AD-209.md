---
suite: admin
edition: both
module: admin-workflows
features:
  - store-settings
priority: medium
title: Branding Settings Persist
tags:
  - admin-workflow
automation:
  implementations:
    - suite: admin
      file: tests/playwright/admin/ad209-branding-settings-persist.spec.ts
---

# Branding Settings Persist

## Scenario

Automated by `tests/playwright/admin/ad209-branding-settings-persist.spec.ts`. This case documents what that test actually does; edit the test first, then regenerate.

## Steps

1. Sign in to WP Admin and open `<site>/wp-admin/admin.php?page=ppcart-settings`.
2. Click `[data-testid="ppcart-admin-settings-tab-branding"]`.

## Expected result

1. `[data-testid="ppcart-admin-settings-search"]` is present on the page.
2. `#content_tab_branding` is present on the page.
3. `[data-testid="ppcart-admin-field-ppcart-company-name"]` is present on the page.
4. `[data-testid="ppcart-admin-field-ppcart-company-address"]` contains "{{admin_workflow_company_address}}".

## Edge cases

- None.
